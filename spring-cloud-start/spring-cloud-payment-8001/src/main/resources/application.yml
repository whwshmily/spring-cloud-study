server:
  port: 8001

spring:
  application:
    name: cloud-payment-8001

  datasource:
    type: com.alibaba.druid.pool.DruidDataSource
    username: root
    password: root
    url: jdbc:mysql://localhost:3306/study_cloud?useSSL=false&useUnicode=true&characterEncoding=UTF-8

mybatis:
  mapper-locations: classpath:mapper/*.xml
  type-aliases-package: com.whw.domain     #所有Entity别名类所在包
